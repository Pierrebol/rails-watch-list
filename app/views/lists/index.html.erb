<div class="container">
  <h1>This is all your lists</h1>
  <%= link_to "Add a list", new_list_path %>

  <ul>
    <% @lists.each do |list| %>
    <li>
      <%= link_to "#{list.name}" , list_path(list) %>
    </li>
    <% end %>
  </ul>
</div>

